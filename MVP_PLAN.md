# 谷子交易与 3D/AIGC 平台：可行 MVP 方案

- **版本**：v2.0
- **状态**：拟实施
- **规划日期**：2026-08-17
- **建议周期**：6 周
- **建议人力**：1–2 名全栈开发；有条件时增加兼职 UI/测试
- **交付定位**：可部署、可注册、数据真实持久化、核心流程可用的全栈 MVP

## 1. MVP 定义

本版本不再以“一天完成”为前提，也不把纯前端原型视为最终 MVP。目标是在可控周期内交付一个具备真实用户、真实数据、真实文件上传和可验证业务状态的全栈系统，用于课程项目、作品集展示、用户访谈和小范围内测。

MVP 要回答三个问题：

1. 用户是否愿意在谷子交易中查看和使用 3D 商品展示？
2. 卖家是否愿意上传商品图片或模型来提高展示质量？
3. AI 建模结果能否自然回流到商品发布，而不是成为孤立功能？

MVP 不等于可直接公开运营。正式支付、资金托管、平台分账、真实物流聚合及完整内容风控仍属于后续试运营阶段。

## 2. 成功标准

### 2.1 产品成功标准

- 至少 20 名目标用户参与内测。
- 至少 60% 的内测用户能够独立完成“浏览 → 3D 查看 → 下单”路径。
- 至少 30% 的卖家测试用户尝试输入文本并创建 AI 建模任务，或上传 GLB 作为补充。
- 3D 商品详情平均停留时间高于普通商品详情，作为后续价值验证指标。
- 内测期间核心流程无阻断级缺陷。

### 2.2 技术成功标准

- 用户、商品、收藏、地址、订单、资产和 AI 任务均持久化到服务端。
- 图片与 GLB 存入对象存储，不依赖浏览器本地缓存。
- 订单状态由服务端状态机控制，关键迁移有事件记录。
- AI 任务异步执行，页面关闭后任务仍可继续并查询结果。
- 系统可通过 `Docker Compose` 在新环境启动。
- 核心接口具备鉴权、参数校验、幂等和基础审计。

## 3. 用户与角色

“用户”是统一账户主体，买家、卖家和创作者是可重叠的业务身份，不要求分别注册。同一账户既可购买商品，也可发布商品和创建 AI 建模任务；创作者同样可以兼具买家、个人卖家或厂家卖家身份。管理员是独立授权角色。

### 3.1 买家

- 注册、登录和维护收货地址。
- 浏览、搜索和筛选商品。
- 查看商品图片与 3D 模型。
- 收藏商品并创建模拟订单。
- 查看订单状态和操作记录。

### 3.2 卖家

- **个人卖家**：发布个人持有的二手闲置商品，维护成色、实拍图、价格和履约信息。
- **厂家卖家**：发布厂家或品牌方提供的全新商品，维护库存、规格、预售信息和履约信息。
- 两类卖家均可发布、编辑、下架自己的商品，上传商品图片和可选 GLB 模型。
- 查看自己发布的商品和收到的订单，并执行模拟发货。

### 3.3 创作者

- 创建文生 3D 或图生 3D 任务。
- 查看任务进度、失败原因和生成结果。
- 将成功结果绑定到新商品。

### 3.4 管理员

MVP 只提供最小管理能力：查看用户与商品、下架违规商品、查看异常 AI 任务。复杂审核流和风控规则不进入 MVP。

## 4. MVP 范围

### 4.1 P0 必须完成

#### A. 用户与权限

- 邮箱或用户名注册登录。
- 密码安全哈希存储。
- 短期 Access Token 与 Refresh Token。
- 权限层区分普通用户与管理员；普通用户可同时执行买家、个人卖家、厂家卖家和创作者流程。
- 用户只能修改自己的商品、资产和订单操作；卖家类型作为业务资料记录，不要求重复注册账户。

#### B. 商品市场

- 商品列表、分页、关键词搜索。
- 按 IP、品类、成色、价格区间筛选。
- 商品详情展示价格、卖家、成色、发货地、描述和图片。
- 收藏与取消收藏。
- 商品类型首版只正式支持固定价现货；预售可作为标签，交换不进入交易流程。

#### C. 商品发布与资产

- 上传 1–6 张 JPG/PNG/WebP 商品图片。
- 可选上传一个 `.glb` 模型。
- 保存原文件、文件元数据和访问地址。
- 创建、编辑、发布、下架商品。
- 校验文件类型、MIME、大小和归属。

#### D. 文件存储服务

- MVP 即接入对象存储：本地开发使用 MinIO，部署环境可使用腾讯云 COS 或其他 S3 兼容服务。
- 存储用户上传的商品图片、参考图片、视频等静态资源，以及 GLB、贴图和 AI 生成结果。
- Go API 的 `Asset` 模块统一生成对象键、校验文件元数据、记录资源归属，并签发上传和访问凭证；MySQL 仅保存资源元数据及业务绑定关系。
- 商品发布、内容发布和 AI 建模通过资产 ID 绑定文件，不直接持久化临时公网地址；公开商品资源可经 CDN 访问，私有或处理中资源使用限时签名 URL。
- 预留图片压缩、视频转码、缩略图生成、内容审核和孤儿文件清理等后续扩展点。

#### E. 3D 商品预览

- 图片视图与 3D 视图切换。
- 支持旋转、缩放、重置视角和全屏。
- 展示模型加载进度、错误提示和图片降级视图。
- GLB 建议不超过 20 MB；超限时拒绝上传并给出提示。

#### F. 模拟交易闭环

- 维护收货地址。
- 创建固定价商品订单。
- 模拟支付成功，不产生真实扣款。
- 卖家模拟发货，买家确认收货。
- 状态为 `PENDING_PAYMENT → PAID → TO_SHIP → SHIPPED → COMPLETED`。
- 待支付订单可取消；每次状态迁移记录订单事件。
- 防止购买自己发布的商品和重复提交订单。

#### G. AI 建模工作台

- MVP 主流程为“用户输入文本 → RAG 检索与 Prompt 优化 → 文本生成 3D → 网页预览”。
- 支持用户查看并确认优化前后的建模描述，避免系统改写偏离原始意图。
- RAG 知识库首版覆盖商品类别、材质、结构、风格和 3D 模型 Prompt 规范；输出结构化建模参数和最终 Prompt。
- 支持“手办、立牌、展示模型”生成类型；参考图输入作为可选增强能力。
- 异步任务状态为 `QUEUED/RUNNING/SUCCEEDED/FAILED/CANCELED`。
- 默认接入一个真实第三方文本生成 3D 供应商；无可用凭据时启用可明确识别的 Mock Provider。
- 展示原始文本、检索摘要、优化结果、最终 Prompt、任务进度、耗时、失败原因和生成结果。
- 生成结果持久化到对象存储，并可一键带入商品发布页；模型仅作为可交互展示资产，不承诺完全还原真实商品。

#### H. 个人中心

- 我的发布、我的收藏、我的购买订单、我的销售订单。
- 最近 AI 任务和结果资产。
- 基础用户资料和地址管理。

#### I. 最小管理能力

- 管理员查看和下架商品。
- 查看用户基础状态。
- 查看并重试失败的 AI 任务。
- 所有管理操作写入审计日志。

### 4.2 P1 有余量再做

- 支付宝沙箱支付与异步回调。
- SSE 推送 AI 任务状态；P0 可使用 3–5 秒轮询。
- 商品浏览历史、分享链接和卖家主页。
- 图片自动压缩、GLB Draco/KTX2 优化。
- 邮件通知和站内通知。
- 简单内容关键词审核。

### 4.3 明确不做

- 正式支付、资金担保、自动分账与提现。
- 退款资金链路和完整账本。
- 求购悬赏、投稿竞标、交换和多人竞价。
- 即时聊天与议价。
- 真实物流 API、电子面单和多物流聚合。
- 自托管 GPU 模型、多 AI 厂商智能路由。
- 复杂推荐算法、信用体系和自动风控。
- 微服务、Kubernetes 和多区域容灾。

## 5. 核心流程

### 5.1 买家流程

注册登录 → 搜索商品 → 查看详情 → 切换 3D → 收藏/立即购买 → 选择地址 → 模拟支付 → 查看订单 → 确认收货。

### 5.2 卖家流程

注册登录 → 上传图片/GLB → 发布商品 → 查看销售订单 → 模拟发货 → 等待买家确认。

### 5.3 AI 建模流程

用户输入文本 → RAG 检索领域知识 → Prompt 优化器生成结构化参数和最终 Prompt → 用户确认或修改 → Go API 创建任务 → Redis Streams 入队 → AI Worker 调用文本生成 3D Provider → 保存结果到对象存储 → 更新任务 → 前端查看 GLB → 发布为商品。

参考图作为后续增强输入；MVP 只要求模型能够生成、加载和旋转查看。

## 6. 技术架构

### 6.1 架构原则

- 使用模块化单体，不为简历展示强行拆微服务。
- AI Worker 独立部署，因为运行时、依赖和扩缩容方式与交易服务不同。
- MySQL 是交易状态的最终事实来源，Redis 不替代事务和唯一约束。
- 前端通过 API 访问业务数据，不再以 `localStorage` 作为主数据库。
- 外部 AI、存储和支付均通过 Adapter 隔离供应商差异。

### 6.2 组件结构

```text
React Web
    |
    v
Go API（Gin + GORM）
    |-- Auth
    |-- Catalog / Product
    |-- Asset
    |-- Favorite
    |-- Order
    |-- Generation Job
    |-- Admin / Audit
    |
    +-- MySQL 8
    +-- Redis / Redis Streams
    +-- MinIO（开发）/ OSS、COS、S3（部署）
                         |
                         v
              Python AI Worker（FastAPI）
              |-- Mock Provider
              +-- Replicate 或 Tripo AI Adapter
```

### 6.3 技术选型

| 层级 | 技术 | 说明 |
| --- | --- | --- |
| Web | React + TypeScript + Vite | SPA 开发和构建 |
| UI | Tailwind CSS + Lucide React | 快速建立统一响应式界面 |
| 路由/状态 | React Router + Zustand + TanStack Query | 路由、本地交互状态和服务端缓存分离 |
| 表单 | React Hook Form + Zod | 表单状态与前端校验 |
| 3D | Three.js + React Three Fiber + Drei | GLB 加载和浏览器交互 |
| API | Go + Gin | REST API、鉴权和业务编排 |
| ORM | GORM | 数据访问；关键查询允许手写 SQL |
| 数据库 | MySQL 8 | 用户、商品、订单和事件数据 |
| 缓存/任务 | Redis + Redis Streams | 限流、任务队列、短期缓存和幂等辅助 |
| AI Worker | Python + FastAPI | AI Provider 调用和结果处理 |
| 文件 | MinIO / S3 兼容对象存储 | 图片、GLB、参考图和生成结果 |
| API 契约 | OpenAPI | 前后端接口协作和调试 |
| 部署 | Docker Compose + Nginx | 开发、测试和演示环境统一 |
| 日志 | 结构化日志 + Request ID | 串联 Web、Go API 与 AI Worker |

### 6.4 为什么 MVP 不使用微服务

1–2 人团队维护多个业务服务会增加接口协调、部署、日志和事务复杂度。用户、商品、订单等模块先在 Go 单体内保持清晰边界；只有 AI Worker 因 Python/GPU/第三方 SDK 依赖而独立。达到明确的并发或团队拆分需求后再服务化。

## 7. 数据与状态设计

### 7.1 最小数据表

- `users`、`refresh_tokens`、`addresses`
- `products`、`product_images`、`favorites`
- `assets`、`asset_files`
- `orders`、`order_items`、`order_events`
- `generation_jobs`、`generation_outputs`
- `audit_logs`

MVP 暂不创建支付账本、退款、悬赏和物流轨迹表，避免出现没有对应业务的空架构。

### 7.2 一致性规则

- 金额统一使用整数分。
- 订单号和 AI 第三方任务号建立唯一索引。
- 创建订单、校验商品状态和占用库存使用数据库事务。
- 状态迁移使用服务层集中校验，禁止控制器直接改状态。
- 重复请求通过客户端幂等键和数据库唯一约束共同防护。
- Redis 只保存可重建的数据和任务协调信息。

## 8. 非功能性要求

### 8.1 性能

- 商品列表 API 在 50 并发下 P95 小于 500 ms。
- 普通网络下首页可交互时间目标小于 3 秒。
- 图片懒加载并提供缩略图；GLB 按需加载。
- 列表必须分页，数据库查询避免 N+1。

### 8.2 安全

- 密码使用 Argon2id 或 bcrypt 哈希。
- Access Token 短期有效，Refresh Token 可撤销。
- 上传校验扩展名、MIME、文件头和大小，并使用服务端生成的对象键。
- 私有资产使用签名 URL，避免永久公开桶。
- 登录、上传和 AI 创建接口实施限流。
- 用户输入和富文本展示必须防止 XSS、路径穿越和恶意文件。

### 8.3 可用性与可观测性

- 核心操作具备加载、空、成功和失败状态。
- API 返回统一错误结构和可追踪 Request ID。
- AI 失败可人工重试，不因页面关闭丢失。
- 记录登录失败、商品操作、订单迁移和管理员操作日志。
- 提供健康检查：Web、API、MySQL、Redis、对象存储和 Worker。

### 8.4 兼容性

- 支持最新版 Chrome、Edge 和 Safari。
- 桌面端优先，移动端支持浏览、详情、购买和任务查询。
- 不支持 WebGL 时自动降级为商品图片。

## 9. 六周实施计划

### 第 1 周：基础与账户

- 确定页面稿、OpenAPI 初稿和数据模型。
- 初始化 React、Go、MySQL、Redis、MinIO 和 Docker Compose。
- 完成注册、登录、Refresh Token、角色和基础布局。

**验收**：两个用户可独立注册登录，鉴权接口和环境启动稳定。

### 第 2 周：商品与资产

- 完成商品列表、搜索、筛选和详情。
- 完成图片上传、GLB 上传、对象存储和商品发布。
- 完成商品编辑、下架和权限校验。

**验收**：卖家发布的真实数据和文件可被其他用户访问。

### 第 3 周：3D 与个人中心

- 完成 3D Viewer、加载状态、错误降级和移动端适配。
- 完成收藏、我的发布和地址管理。
- 补充文件限制与安全校验。

**验收**：GLB 可稳定加载，失败时不阻断商品浏览。

### 第 4 周：订单闭环

- 完成创建订单、模拟支付、卖家发货、买家确认收货。
- 完成订单状态机、库存条件更新、幂等与订单事件。
- 完成买家订单和卖家订单页面。

**验收**：两个账号能完成一笔完整模拟交易，非法状态迁移被拒绝。

### 第 5 周：RAG 与文本生成 3D

- 完成领域知识库、检索流程和 Prompt 优化器。
- 完成原始文本、检索内容、结构化参数和最终 Prompt 的保存与展示。
- 完成 Redis Streams 任务队列和 Python AI Worker。
- 完成 Mock Provider，并接入一个真实文本生成 3D Provider。
- 完成任务轮询、失败重试、结果入库和一键发布。

**验收**：用户输入文本后可以看到优化结果并确认提交；关闭页面后任务继续执行，结果可重新打开、预览并发布为商品。

### 第 6 周：管理、质量与部署

- 完成最小管理后台和审计日志。
- 完成响应式、错误状态、基础测试和演示数据。
- 完成 Docker Compose 部署、备份说明和内测发布。
- 邀请目标用户执行测试任务并记录指标。

**验收**：新环境可按文档启动，核心路径无阻断级问题，具备内测条件。

## 10. 测试与验收

### 10.1 必测场景

- 注册、登录、刷新令牌和越权访问。
- 图片/GLB 上传的正常、超限、伪造类型和失败场景。
- 商品搜索、分页、发布、编辑和下架。
- 3D 加载、操作、失败和 WebGL 降级。
- 重复创建订单、购买自己商品、库存不足和非法状态迁移。
- AI 成功、失败、超时、重试、取消和重复回调。
- 管理员下架商品与普通用户越权操作。

### 10.2 发布验收路径

1. 用户 A 注册并发布带 GLB 的手办商品。
2. 用户 B 登录、搜索商品并查看 3D。
3. 用户 B 收藏并创建模拟订单。
4. 用户 A 模拟发货，用户 B 确认收货。
5. 用户 A 创建真实或 Mock AI 任务。
6. AI 结果生成后绑定到新商品并发布。
7. 管理员查看审计记录并下架测试商品。

以上流程必须在服务重启和页面刷新后保持数据一致。

## 11. 资源与成本预估

### 11.1 人力

- 1 人：建议 7–8 周，并进一步降低管理后台视觉要求。
- 2 人：建议前端/3D 与后端/AI 分工，6 周可行。
- UI 与测试可兼职参与关键评审和发布前验收。

### 11.2 外部依赖

- 一个 S3 兼容对象存储环境。
- 一个可用域名和 HTTPS 环境，用于内测。
- 一个 Replicate 或 Tripo AI 测试账号；无账号时 Mock Provider 不阻塞开发。
- 8–12 个来源明确的商品素材和 2–3 个经过验证的 GLB。

## 12. 风险与降级策略

| 风险 | 应对 | 降级方式 |
| --- | --- | --- |
| AI API 不稳定或成本超限 | Provider Adapter、超时和成本上限 | 切换 Mock Provider，保留完整异步链路 |
| GLB 质量不一致 | 上传限制、预览检查和示例模型 | 仅展示图片，不阻断商品发布 |
| 六周进度延误 | 每周按可演示垂直切片验收 | 先取消管理员 UI，再取消真实 AI Provider |
| 支付与合规复杂 | MVP 只做明确标注的模拟支付 | 不接任何真实资金能力 |
| 版权素材不足 | 提前准备自有或授权素材 | 使用原创占位素材进行内测 |
| 单人同时维护 Go/Python/React 压力大 | AI Worker 仅保留必要逻辑 | 由 Go 消费任务并调用外部 API，后续再拆 Python |

## 13. MVP 完成定义

同时满足以下条件才视为 MVP 完成：

- 不是纯静态演示，核心数据由 Go API 和 MySQL 持久化。
- 两个真实账号可完成发布、浏览、3D 查看和模拟交易。
- 图片与 GLB 使用对象存储，可在不同设备访问。
- AI 任务采用真实异步链路，至少 Mock Provider 稳定可用。
- 订单状态机、权限和关键审计日志通过测试。
- 项目可部署到测试环境并供目标用户访问。
- 已完成至少一轮目标用户测试并形成问题清单。

## 14. 后续版本

MVP 验证通过后，再按优先级增加：支付宝沙箱、退款与账本、求购悬赏、通知、内容审核、真实物流、卖家信用和合规分账。只有在业务量、团队规模或独立扩容需求明确后，才拆分用户、交易、支付等微服务。
