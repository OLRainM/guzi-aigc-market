# 谷子交易与 3D/AIGC 展示平台 PRD

- **文档版本**：v1.1
- **文档状态**：Draft
- **编写日期**：2026-08-17
- **项目定位**：二次元谷子交易平台，3D 商品预览与 AI 建模为差异化能力
- **当前实施基线**：[MVP_PLAN.md](./MVP_PLAN.md)，按 1–2 人、约 6 周交付真实持久化的全栈 MVP

## 1. 项目背景与目标

### 1.1 项目背景

传统谷子交易主要依赖静态图片、文字描述和私聊沟通，买家难以判断手办、立牌等立体商品的外观，卖家也缺少低成本展示商品的工具。同时，AIGC 生成 3D 模型的能力逐渐成熟，可以将商品图片或创意描述转化为可交互的 3D 展示资产。

本项目不把 3D 模型本身作为唯一交易对象，而是以谷子交易为主，以 3D 预览提升商品信息完整度，以 AI 建模工作台形成产品记忆点。

### 1.2 产品目标

1. 建立一个可注册、浏览、搜索、发布和模拟购买谷子的全栈产品。
2. 让支持模型的商品可以在浏览器内旋转、缩放和查看。
3. 实现“文本输入 → RAG 检索与 Prompt 优化 → 异步文本生成 3D → 生成结果 → 发布商品”的完整流程；参考图片作为后续增强输入。
4. 使用 Go 模块化单体与独立 AI Worker，实现真实用户、资产、订单和任务持久化。
5. 在约六周内交付可部署、可供目标用户内测且能够继续迭代的 MVP。

### 1.3 非目标

当前 MVP 不实现正式支付、真实分账、即时聊天、真实物流、自部署 GPU 模型、复杂竞拍、完整风控和生产级多租户能力。

## 2. 目标用户

### 2.1 核心用户

- **谷子买家**：浏览商品、筛选 IP 和品类、查看 3D 效果、收藏并模拟下单。
- **谷子卖家**：发布现货、预售或二手商品，补充商品信息和 3D 模型。
- **创作者**：通过 AI 建模生成展示模型，并将结果转为商品。

### 2.2 管理用户

- **运营管理员**：管理商品、用户、举报、内容审核和订单异常。
- **平台管理员**：维护 AI 配置、支付配置、物流适配器和系统监控。

### 2.3 用户痛点

| 用户 | 痛点 | 产品解决方式 |
| --- | --- | --- |
| 买家 | 静态图难以判断立体商品外观 | 商品详情提供 3D 预览 |
| 卖家 | 制作高质量展示内容成本高 | AI 建模工作台生成可交互模型 |
| 创作者 | 作品展示和交易链路割裂 | AI 结果一键发布为商品 |
| 平台 | 交易、生成和履约状态复杂 | 领域模块与状态机分离管理 |

## 3. 产品价值主张

> 让用户用自然语言描述创意，通过领域知识增强生成可交互的 3D 展示模型，让买家在购买前更直观地了解商品。

AI 建模不是独立的炫技页面，而是服务商品展示和交易转化的能力。MVP 重点验证“文本输入 → RAG 优化 → 文本生成 3D → 网页预览”的链路，至少提供稳定的 Mock Provider，并在具备凭据时接入一个真实文本生成 3D Provider。

## 4. 产品范围与优先级

### 4.1 P0：六周 MVP 必须完成

1. 真实注册登录、基础角色与权限校验。
2. 商品市场：分页、关键词搜索以及 IP、品类、成色和价格筛选。
3. 商品发布：真实图片上传、可选 GLB 上传、编辑和下架。
4. 商品详情：图片、价格、卖家、成色、描述、收藏和 3D 预览。
5. 模拟交易：地址、模拟支付、买卖双方订单、模拟发货和确认收货。
6. AI 建模：异步任务、Mock/真实 Provider、结果持久化和一键发布。
7. 个人中心：发布、收藏、购买订单、销售订单和 AI 任务。
8. 最小管理能力：商品下架、用户查看、失败任务重试和审计日志。

### 4.2 P1：有余量再做

- 支付宝沙箱与异步回调。
- SSE 任务推送、邮件/站内通知、卖家主页和分享。
- 图片压缩、GLB Draco/KTX2 优化和简单内容审核。

### 4.3 P2：试运营能力

- 退款、支付账本和合规分账。
- 求购悬赏、投稿选稿、交换和议价。
- 真实物流适配、举报审核、信用体系和运营分析。

## 5. 核心用户流程

### 5.1 买家购买流程

进入市场 → 搜索/筛选 → 查看商品详情 → 图片与 3D 预览切换 → 收藏或立即购买 → 填写地址 → 查看演示支付提示 → 创建订单 → 个人中心查看订单。

### 5.2 卖家发布流程

进入发布页 → 选择/上传封面 → 填写商品信息 → 绑定预置 GLB（可选）→ 发布 → 商品出现在市场 → 查看我的发布。

### 5.3 AI 建模流程

进入 AI 建模 → 输入文本描述 → RAG 检索领域知识 → 生成结构化建模参数和优化 Prompt → 用户确认或修改 → 选择模型类型 → 创建异步任务 → 排队 → 生成中 → 完成 → 预览 GLB → 填写商品信息 → 一键发布。

参考图片作为可选输入，用于后续提高生成结果与用户意图的一致性。MVP 重点验证文本到 3D 的完整链路，模型“能生成、能加载、能旋转查看”即可，不以生产级精度作为首版门槛。

## 6. 功能需求

### 6.1 市场首页

- 展示商品卡片：封面、标题、IP、品类、价格、成色、交易类型、3D 标识。
- 支持关键词搜索、IP 分类、商品类型、价格区间和成色筛选。
- 支持在售、预售、交换标签；首版交换仅作展示标签，不进入真实交易闭环。
- 无结果时显示空状态，并提供清除筛选操作。

### 6.2 商品详情与 3D Viewer

- 展示商品图片、描述、卖家、发货地、成色和价格。
- 具备图片视图和 3D 视图切换。
- 3D 支持拖拽旋转、滚轮缩放、重置相机、加载进度和错误状态。
- 模型优先使用 `.glb/.gltf`；首版限制模型大小和面数，优先使用预置模型。
- 明确展示“3D 展示模型不代表实物尺寸或材质完全一致”的提示。

### 6.3 商品发布

- 必填：标题、分类、价格、成色、交易类型、封面、描述。
- 可选：绑定 3D 模型、发货地、库存、预售说明。
- 发布前校验标题长度、价格范围、图片格式和描述内容。
- MVP 发布成功后立即可见；生产版进入审核状态。

### 6.4 模拟订单

- 创建订单前确认商品、数量、金额和收货地址。
- 金额使用整数分或 Decimal 的领域规则，前端不使用浮点计算关键金额。
- 模拟支付页面必须有“演示支付，不产生真实扣款”标识。
- 订单状态：待支付、已支付、待发货、运输中、已完成、已取消。
- 生产版由服务端校验状态迁移，并记录订单事件。

### 6.5 AI 建模工作台

- MVP 主入口为文本描述，支持用户输入想生成的手办、立牌或展示模型。
- 系统通过 RAG 检索商品类别、材质、结构、风格和 3D Prompt 规范等领域知识。
- Prompt 优化器输出结构化建模参数和最终 Prompt；用户可以查看、修改并确认后再提交生成。
- 参考图片作为后续增强输入，不作为 MVP 主链路依赖。
- 任务状态：`QUEUED`、`RUNNING`、`SUCCEEDED`、`FAILED`、`CANCELED`。
- 展示原始文本、检索摘要、优化描述、最终 Prompt、任务创建时间、当前阶段、预计等待提示和失败原因。
- MVP 使用 Mock Provider 返回预置 GLB；具备凭据时接入文本生成 3D 供应商。
- 成功结果可以预览、重新生成或发布为商品；首版只要求能加载、旋转和查看，不承诺完全还原真实商品。
- 保存 Prompt、RAG 知识版本、模型版本、生成参数和用户版权确认记录。

### 6.6 个人中心

- 展示我的发布、我的收藏、我的订单和最近 AI 任务。
- MVP 使用演示用户；第二阶段接入 JWT 登录和真实用户数据。
- 提供清空演示数据或恢复初始数据入口，便于重复演示。

### 6.7 管理后台

- 商品上下架、用户封禁、内容审核、举报处理。
- 订单异常、退款申请、支付流水和物流异常处理。
- AI 供应商配置、调用量、失败率和成本统计。

## 7. 优化后的技术架构

### 7.1 分阶段架构

```text
React Web（TypeScript + Vite）
             |
             v
       Go API（Gin + GORM）
       |-- 用户与权限
       |-- 商品与资产
       |-- 收藏与地址
       |-- 订单与状态机
       |-- AI 任务与管理审计
       |
       +-- MySQL 8：核心业务数据
       +-- Redis：限流、缓存与任务协调
       +-- MinIO/OSS/COS/S3：图片、GLB、参考图与生成结果
       +-- Redis Streams：异步任务
                    |
                    v
       Python AI Worker（FastAPI）
       |-- RAG 检索与 Prompt 优化器
       |-- Mock Provider
       |-- 文本生成 3D Provider Adapter
       |-- 文件下载、校验与结果处理
       |-- 任务状态与结果回传
```

MVP 从首个版本即使用全栈架构；求购、支付账本和真实物流等模块在验证核心价值后再加入。

### 7.2 技术选型说明

| 领域 | 选型 | 原因 |
| --- | --- | --- |
| 前端 | React + TypeScript + Vite | 适合构建类型安全的响应式 SPA |
| 路由/状态 | React Router + Zustand + TanStack Query | 分离路由、本地交互状态和服务端缓存 |
| 表单 | React Hook Form + Zod | 统一表单状态与前端校验 |
| UI | Tailwind CSS + Lucide React | 快速实现统一响应式界面 |
| 3D | Three.js + React Three Fiber + Drei | 适合 GLB 加载、相机控制和交互展示 |
| 业务服务 | Go + Gin | 性能稳定、并发模型清晰，适合交易 API |
| 数据访问 | GORM + MySQL 8 | 降低 CRUD 成本，同时保留事务和索引控制能力 |
| AI Worker | Python + FastAPI | 负责 RAG 检索、Prompt 优化、AI SDK 调用、文件处理和后续 PyTorch 自托管 |
| 异步任务 | Redis Streams | 满足 MVP 的持久任务、消费组和重试需求 |
| 文件存储 | MinIO/OSS/COS/S3 | 支持大文件、签名 URL 和 CDN 演进 |
| 支付 | MVP 模拟支付；P1 支付宝沙箱 | 不在 MVP 中产生真实资金风险 |
| 部署 | Docker Compose + Nginx | 统一 Web、API、Worker 和基础设施环境 |
| 观测 | 结构化日志 + Request ID；后续接 OpenTelemetry | 先保证核心链路可定位，再扩展指标平台 |

### 7.3 后端模块边界

- `auth`：用户、角色、JWT、权限。
- `catalog`：商品、分类、搜索和上下架。
- `asset`：图片、GLB、版本、元数据和签名 URL。
- `generation`：AI 任务、供应商适配和结果资产。
- `listing`：固定价出售和求购悬赏。
- `order`：订单、库存、状态机和事件。
- `payment`：支付流水、回调、退款和账本。
- `logistics`：发货、轨迹和签收。
- `notification`：站内通知和 SSE 推送。
- `admin`：审核、举报、风控和运营配置。

### 7.4 核心数据实体

`users`、`roles`、`assets`、`asset_files`、`asset_versions`、`products`、`sale_listings`、`bounties`、`bounty_submissions`、`generation_jobs`、`generation_outputs`、`orders`、`order_items`、`order_events`、`payment_transactions`、`refunds`、`ledger_entries`、`shipments`、`notifications`、`audit_logs`。

订单号、支付平台交易号、AI 第三方任务号必须建立唯一索引。库存扣减、支付回调、选稿和交付需要事务、幂等键和审计日志。

## 8. API 规划

### 8.1 MVP 模拟接口

首版可使用前端 Adapter 保持接口形状：

- `GET /api/products`
- `GET /api/products/:id`
- `POST /api/products`
- `POST /api/orders`
- `GET /api/orders`
- `POST /api/generation-jobs`
- `GET /api/generation-jobs/:id`
- `POST /api/generation-jobs/:id/publish`

### 8.2 生产接口要求

- 所有写接口使用请求 ID 或幂等键。
- 支付回调必须验签、核验商户订单号和金额，并允许重复回调。
- AI 回调必须验签或使用安全回调凭证，状态更新必须幂等。
- 高风险操作记录操作者、请求来源、前后状态和时间。

## 9. 非功能性需求

### 9.1 性能

- 首页首屏目标：普通网络环境下可交互时间不超过 3 秒。
- 商品列表接口 P95 不超过 500 ms（演示环境除外）。
- 3D 模型首版建议单文件不超过 20 MB，生产版通过 Draco/KTX2 压缩。
- 列表使用分页或游标，避免一次加载全部商品。

### 9.2 可用性

- 关键页面支持桌面端和移动端基本访问。
- 所有异步操作具备加载、成功、失败和空状态。
- AI 任务失败可重试，支付回调重复不会重复创建订单或扣款。
- 前端刷新后，演示收藏、发布和订单信息仍可恢复；大文件使用 IndexedDB 或预置素材。

### 9.3 安全

- JWT 使用短期 Access Token 和可撤销 Refresh Token。
- 上传校验扩展名、MIME、文件头、大小和压缩包内容，防止路径穿越与压缩炸弹。
- 私有文件只通过签名 URL 访问。
- 接口实施 RBAC、限流、幂等和审计。
- 禁止将演示支付描述为真实扣款。

### 9.4 合规

- 商品图片、角色/IP、模型和生成素材必须有明确来源或授权。
- AIGC 任务保留模型版本、提示词、参考图来源和用户版权确认。
- 正式上线前评估仿品、盗版、预售、未成年人交易、消费者退款、隐私和平台分账责任。
- 生产版不得用普通转账模拟平台担保或分账。

### 9.5 可维护性

- 前后端领域模型保持稳定，模拟 Adapter 可替换为 HTTP API。
- 业务状态迁移集中实现，禁止在多个控制器中散落状态修改。
- 使用 OpenAPI 维护接口契约，关键流程补充集成测试和冒烟测试。

## 10. 视觉与交互要求

- 整体风格：年轻、收藏感、轻量科技感，不使用过度复杂的 3D 首页。
- 商品卡片优先突出封面、价格、品类、成色和 3D 标识。
- 3D Viewer 必须有清晰的操作提示、加载状态和错误兜底。
- 演示支付、模拟物流和模拟 AI 均使用明显但不打扰阅读的标签。
- 移动端优先保证筛选、详情、购买和 AI 提交按钮可操作。

## 11. 数据与状态规则

### 11.1 商品状态

`DRAFT` → `PUBLISHED` → `OFF_SHELF`；生产版增加 `PENDING_REVIEW` 和 `REJECTED`。

### 11.2 订单状态

`PENDING_PAYMENT` → `PAID` → `TO_SHIP` → `SHIPPED` → `COMPLETED`；允许从待支付取消，生产版增加退款状态。

### 11.3 AI 任务状态

`QUEUED` → `RUNNING` → `SUCCEEDED` 或 `FAILED`；运行中允许取消，失败允许按策略重试。

商品、订单、物流和 AI 状态必须分开维护，不能用一张状态字段覆盖不同领域含义。

## 12. 里程碑规划

| 里程碑 | 周期 | 交付内容 | 验收标准 |
| --- | --- | --- | --- |
| M1 账户与基础 | 第 1 周 | 环境、数据模型、注册登录、角色权限 | 多用户可独立登录，鉴权与部署环境稳定 |
| M2 商品与资产 | 第 2 周 | 商品 API、搜索筛选、图片/GLB 上传与发布 | 卖家真实文件和商品可被其他用户访问 |
| M3 3D 与个人中心 | 第 3 周 | 3D Viewer、收藏、地址、我的发布 | 模型可稳定交互，失败时能降级为图片 |
| M4 模拟交易 | 第 4 周 | 订单、状态机、模拟支付/发货/收货、事件日志 | 两个账号可完成交易，非法迁移被拒绝 |
| M5 异步 AI | 第 5 周 | Redis Streams、AI Worker、Provider、结果发布 | 页面关闭后任务继续，结果可发布为商品 |
| M6 管理与内测 | 第 6 周 | 最小管理、审计、质量修复、部署和用户测试 | 新环境可启动，核心路径无阻断缺陷 |
| M7 试运营 | MVP 后 4–8 周 | 沙箱支付、通知、审核、退款和物流适配 | 具备小范围灰度运行条件 |
| M8 生产化 | 3–6 个月 | 合规分账、风控、CDN、灾备和弹性扩展 | 完成安全、性能、合规和运维评审 |

## 13. 风险与应对

| 风险 | 等级 | 应对 |
| --- | --- | --- |
| 无可用 GLB 或模型过大 | 高 | 上传限制、预览检查、示例模型和图片降级 |
| AI 供应商接口或余额不可用 | 高 | 使用 Mock Provider 与统一 Adapter；先验证 RAG 和任务链路，不阻塞异步流程 |
| 图片和 IP 版权不清晰 | 高 | 使用自有/授权素材，保存来源和版权确认 |
| 支付分账缺少资质 | 高 | MVP 明确模拟，生产接入合规产品并先完成法务评估 |
| 订单状态不一致 | 高 | MySQL 事务、唯一索引、幂等键和事件日志 |
| 六周范围失控 | 中 | 坚持每周垂直验收，优先取消管理 UI 和真实 AI Provider |
| 3D 移动端性能不足 | 中 | GLB 压缩、懒加载、低面数模型和加载失败兜底 |

## 14. MVP 验收标准

1. 两个真实账号可以独立注册登录，并受到正确的资源权限约束。
2. 卖家上传的图片与 GLB 存入对象存储，其他设备可正常访问。
3. 买家可以搜索商品，在图片和 3D 视图间切换，并收藏商品。
4. 买卖双方可以完成模拟支付、发货与确认收货，非法状态迁移会被拒绝。
5. AI 任务采用 Redis Streams 异步执行；用户输入文本后，系统能够完成 RAG 检索、Prompt 优化、任务提交和状态查询，页面关闭后仍可继续执行。
6. AI 结果可以持久化、预览并发布为新商品；首版模型只需能加载、旋转和查看。
7. 服务重启和页面刷新后用户、商品、订单、资产和任务数据保持一致。
8. 管理员操作有审计记录，项目可通过 Docker Compose 在新环境启动。
9. 至少完成一轮目标用户内测，核心流程无阻断级缺陷。

## 15. 结论

本项目采用“六周全栈 MVP → 试运营能力 → 生产化平台”的路线。首版即使用 Go 模块化单体、MySQL、Redis、对象存储和独立 AI Worker，但将正式支付、分账、退款、求购悬赏和真实物流留到验证核心价值之后。

首要成功标准不是服务数量，而是用一条完整、可信、可持久化的路径证明：**谷子商品可以被发现，3D 能力可以增强理解，AI 结果可以回流到商品交易场景。**
